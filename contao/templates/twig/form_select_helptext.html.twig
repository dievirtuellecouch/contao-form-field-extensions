{% extends '@Contao/form_select.html.twig' %}

{% block label %}
    {{ parent() }}
    {% if this.helpText|default %}
        <p>{{ this.helpText|insert_tag_raw }}</p>
    {% endif %}
{% endblock %}
